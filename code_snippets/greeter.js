let name = 'Victor';

console.log(`Good morning ${name}`);
console.log(`Good afternoon ${name}`);
console.log(`Good evening ${name}`);

/*
  On line 1 we declare a globally scoped variable 'name'.
  Then we use an initializer to store the string value of 'Victor'
  in 'name'. On lines 3 to 5 we console.log a template literal
  passing in 'name'
 */